hello_world:
  tags:
    - otus
  variables:
    SCHEDULER_PARAMETERS: -A $SLURM_ACCOUNT -p normal -c 16 --mem 16G -t 00:05:00
  script:
    - echo "Hello World from $(hostname)! I have $(nproc) cores!"

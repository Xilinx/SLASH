name: Push PR to Gitlab CI/CD
on:
  - pull_request
  #pull_request_review:
  #  types: [edited, submitted]
  
jobs:
  Push-PR:
    #if: github.event.review.state == 'approved'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - run: |
          echo config user.name "github-actions[bot]"
          echo config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          echo "${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}" $GITHUB_REF_NAME
      #- run: git push "${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}" 
